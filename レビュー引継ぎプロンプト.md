# Vue.js 3教材：再レビュー用・引継ぎプロンプト

以下の前提を守り、Vue.js 3の企業研修教材を再レビューしてください。

## 目的・対象・報告方法

- 対象は第1章〜第5章の最新版の教材本文、演習ガイド、問題、解答、配布用ソースです。
- 受講者は初学者です。技術的な正確さに加え、手順どおりにコピーして実行できること、学習順序が自然であることを重視してください。
- レビューだけを行い、明示的な依頼がない限り、ファイル・Word文書・DBを変更しないでください。
- node_modules、ビルド成果物、個人的な検証用ファイル、Wordの一時ファイルは対象外です。
- 各指摘は、重要度、該当箇所、現状、問題の根拠、最小修正案、確認方法を必ず含めてください。
- 推測で指摘せず、ソース・本文・ガイドを突き合わせ、通常・解答プロジェクトは必要に応じてビルドとAPI実行で再現確認してください。

## 全章共通：再指摘しない前提

- question、_qなどの問題プロジェクトは穴埋め問題です。空欄・未使用変数・未実装関数に起因するビルド失敗は意図した状態であり、バグとして指摘しません。通常プロジェクトと解答プロジェクトのビルド結果とは区別してください。
- Node.jsの依存関係は固定バージョンを正とします。安易な最新化、メジャー更新、バージョン範囲への変更を提案しません。通常・問題・解答の対応する依存関係が一致するかを確認してください。
- 最新の直接依存関係では解消できない推移的依存関係の監査警告は、教材の重複ISSUEにしません。現実的な直接依存関係の修正がある場合だけ、影響とともに報告してください。
- 初学者の理解を優先します。未学習の型、過度な抽象化、共通化、複雑なREST設計の導入は提案しません。
- 画像は参考用の場合があります。画像とコードの差だけで誤りとせず、教材手順との矛盾や実行不能がある場合だけ指摘してください。
- パスワードをサンプルデータや画面で扱うことは、認証を扱わないCRUD学習用の意図的な簡略化です。セキュリティ欠陥として指摘しません。

## 第1章

- BootstrapVueNextは教材・ソースコードから削除する方針です。純粋なBootstrap 5で第5章までの学習内容を扱います。
- ESLintは研修で必須としない方針です。導入不足や設定不足をISSUEにしません。
- Django関連は研修用ではない個人的な検証ファイルです。教材の欠落として扱わず、レビュー対象外です。
- 空フォルダは不要です。教材に必要のない空フォルダを見つけた場合だけ、削除候補として報告してください。

## 第2章

- Vueを使わない素のJavaScript例は、Vueの書き方ではないことを問題視しません。
- Viteが生成するApp.vueは、script、template、styleのタグ枠を残し、中身を削除して開始する説明を正とします。
- React・Vue・Angularの市場シェア比較に関する記述は削除する方針です。古い市場シェア情報の再追加を提案しません。
- 算術処理の型指定は、computedの戻り値をnumberと明示する書き方へ統一する方針です。
- styleバインディングの説明は、:style="プロパティ名: 値"を正とします。
- 部署名は営業部を正とします。

## 第3章

- refを使う説明・ソースコードを正とします。
- 重複する既存の名前表示は削除する方針です。
- importに付随する既存コメントは、初学者に有益なため現状維持を許容します。
- 子のイベント名selectは意図的です。@selectとemit('select')が連動し、親のハンドラーの仮引数で値を受け取る説明を正とします。
- 親はsrc直下のApp.vue、子はsrc/componentsに置く構成例を正とします。
- コンポーネントのimport名は、Vueファイル名に対応し、大文字から始まる名前にする説明を正とします。
- :keyには安定して各項目を特定するため、なるべくidを指定する説明を正とします。
- Main.vueは教材上の変化を見せるためファイル自体を残します。ただし旧内容は残さず、AppからEmployeeCardを直接配置する旨のコメントだけを置く方針です。
- 画像が正と明示された設問は、問題・解答・本文を画像側へ揃える方針です。

## 第4章

- ルーター登録はmain.tsでrouterをimportし、createApp(App).use(router).mount('#app')とする構成を正とします。
- 第3章との差を見せるため、該当時点のprops・emit関連を最初からコメントアウトする方針を許容します。
- ダッシュボード表示用のDummyルートは許容します。未学習のRouteRecordRawを導入して解決する提案はしません。必要なら、ソース内コメントでDummyをHomeViewのルートへ置換する手順を示します。
- 初学者にとって分かりやすい固定値を優先します。複雑な動的化や共通化を理由に変更を強く勧めません。
- createWebHistory()を正とします。BASE_URLを渡す形へ戻す提案はしません。
- 研修で説明していない後続導線を新設する提案は必須ISSUEにしません。
- 第4.5節のphotoUrlは使用していないため削除する方針です。
- 表記はRouterLinkを正とし、不要な記号は削除する方針です。

## 第5章

- 検索APIは/api/employees/search/、全件取得APIは/api/employees/です。初期表示は全件取得、検索ボタンは検索APIを呼ぶ構成を正とします。
- テーブル名はemployeesに統一します。SQLのDROP、CREATE、APIのSQLが一致するか確認してください。ただし、演習ガイドにテーブル名の直接記載がない場合、無理に追記する必要はありません。
- 商品の初期データは、ノートパソコン120000電子機器、スマートフォン90000電子機器、オフィスチェア25000家具、ボールペンセット800文房具の4件を正とします。
- 社員一覧の動作確認URLはhttp://localhost:5173/employeesです。
- REST APIの説明が初学者向けに矛盾しない範囲であれば、表現だけを理由に設計変更を提案しません。
- 存在しないIDの詳細取得・更新・削除は404を返します。この内容は本文を大きく変えず、ソースコード中の※補足：コメントで説明する方針です。
- 演習5.3・5.4フォームの一部はv-modelを補う穴埋め問題です。未実装バグとして報告しません。
- 演習ガイドで使用する共通コンポーネント名はHeaderMenu.vue、SideMenu.vue、FooterInfo.vueです。
- MainContent.vueは旧来の固定データ・props・emitの例を廃止する変化を見せるため、ファイルを残します。内容は廃止を案内するコメントのみを正とします。
- 画面のフッター下寄せはBootstrapのmin-vh-100、d-flex、flex-column、flex-grow-1を使います。※補足：コメントによる説明を正とします。
- フッター年は2026に統一します。

## 引き続き厳しく確認する観点

- 本文、スクリーンショット、演習ガイド、問題、解答、配布ソース間に、上記方針に反する実行不能な不一致がないか。
- コマンド、ファイル名、URL、ルート名、コンポーネント名、importパス、APIエンドポイント、SQLiteのテーブル名・カラム名が一致するか。
- 通常・解答プロジェクトがクリーンな依存関係導入後にビルドできるか。
- APIの一覧、検索、詳細、登録、更新、削除が教材どおりに動くか。変更系を検証した場合は初期SQLを再適用して初期状態へ戻してください。
- 初学者が、説明されていない前提知識や操作なしに進められるか。
- 日本語の誤字、表記ゆれ、全角半角、年号、コードブロックのコピー不能な改行を確認してください。

## 最終注意

- 上記の意図的な方針をISSUEとして再起票しないでください。該当するときは「意図的な方針として確認済み」とだけ記録してください。
- 方針と異なる重大な問題を見つけたときは、再現手順と最小修正案を添えて報告してください。
- 既存のユーザー作業やWord文書を無断で削除・上書きしないでください。
